{
  "//": "Type-checks src + tests together (the base tsconfig excludes test/ so the published build never includes it). Mirrors sdk/tsconfig.test.json — see ../sdk/STANDARDS.md §6.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "rootDir": ".",
    "noEmit": true
  },
  "include": ["src/**/*", "test/**/*"],
  "exclude": ["node_modules", "dist"]
}
